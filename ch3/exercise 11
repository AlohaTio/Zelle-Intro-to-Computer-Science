#Python Programming Third Edition
#Chapter 3, Exercise 11
	#A program to find the sum of the first n natural numbers

def main():
    #Describe the program to the user
    print("This program finds the sum of the first 'n' numbers")
    print("The 'n' is supplied by the user")
    print()

    sum = 0
    #Get the inputs from the user
    n = int(input("What 'n' would you like to use?: "))

    #Calculate the sum of the sequence
    for k in range(1, n + 1):
        
        sum = sum + k

    print("The sum of the fist", n, "numbers is", sum)
main()    
